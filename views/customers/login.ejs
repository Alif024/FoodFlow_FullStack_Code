<%- include('../partials/header') %>

  <div style="max-width:420px; margin:60px auto;">
    <div class="form">
      <h2 style="margin-top:0; color:var(--text);">Customer — Table Login</h2>

      <form action="/customer-login" method="POST">
        <label for="tableNumber">หมายเลขโต๊ะ (Table number)</label>
        <input id="tableNumber" name="tableNumber" type="number" min="1" required placeholder="e.g. 12">

        <div style="margin-top:14px;">
          <button class="btn btn-primary" type="submit">เข้าสู่ระบบ</button>
        </div>
      </form>
    </div>
  </div>

<%- include('../partials/footer') %>
